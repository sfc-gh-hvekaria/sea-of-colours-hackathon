[pytest]
pythonpath = .
# Both trees, or a bare `pytest` silently skips the ~737 orchestrator
# tests — including V12's, the agent the hackathon is actually about.
testpaths = tests sea_of_colours/orchestrator_2/tests turnlab/tests
